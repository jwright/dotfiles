# Everything downstream of a working 1Password.
# Run with: brew bundle --file=~/Projects/dotfiles/Brewfile

tap "1password/tap"

# --- already installed by bootstrap.sh, listed so `brew bundle` is complete ---
cask "1password"
cask "1password-cli"

# --- shell + core ---
brew "git"
brew "gh"
brew "fzf"
brew "ripgrep"
brew "jq"
brew "tree"
brew "wget"

# --- ruby ---
brew "openssl@3"
brew "libyaml"
brew "asdf"          # or rbenv/mise — swap to taste

# --- elixir ---
brew "erlang"
brew "elixir"
brew "inotify-tools"

# --- javascript ---
brew "node"

# --- databases ---
brew "postgresql@16"
brew "redis"

# --- apps ---
cask "ghostty"
cask "visual-studio-code"
cask "docker"
cask "slack"
